/*
    // Tuple in TypeScript

    It is same as the array additionally
        - the number of elements in tuple is fixed.
        - Types of elements are known, and must not be same.
     

*/
var bgColor, headerColor;
bgColor = [0, 255, 255, 0.5];
headerColor = [0, 255, 255];
console.log(bgColor);
console.log(headerColor);
