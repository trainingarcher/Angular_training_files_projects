// TypeScript object types
/*  These types represents all the values that are not primitive Types.

         The following are primitive types in TypeScript:

             number
             bigint
             string
             boolean
             null
             undefined
             symbol



 // decl. of variable which holds object

let myemployee : object;

myemployee = {
 name : 'anand pande',
 age : 35,
 jd : 'UI Developer'
};

console.log(myemployee);

*/
// can be used as
var myemployee = {
    name: 'Rajesh Thote',
    age: 45,
    jd: 'Backend Developer'
};
console.log(myemployee);
